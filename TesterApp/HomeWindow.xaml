<Window x:Class="TesterApp.HomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TesterApp - Home"
        Width="900"
        Height="650"
        Loaded="HomeWindow_Loaded"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock FontSize="28"
                       FontWeight="Bold"
                       Text="Kenya Home Screen" />
            <Button Grid.Column="1"
                    Width="100"
                    Height="34"
                    Click="BackToMainButton_Click"
                    Content="Back" />
        </Grid>

        <TextBlock x:Name="StatusTextBlock"
                   Grid.Row="1"
                   Margin="0,10,0,14"
                   Foreground="#475569"
                   Text="Loading Kenya information from Wikipedia..." />

        <Border Grid.Row="2"
                Padding="16"
                Background="White"
                BorderBrush="#CBD5E1"
                BorderThickness="1"
                CornerRadius="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="KenyaInfoTextBlock"
                           TextWrapping="Wrap"
                           FontSize="15"
                           LineHeight="26" />
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
